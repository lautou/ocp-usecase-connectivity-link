apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # RBAC for Job to manage Gateway resources
  - ../../base/cluster-clusterrole-gateway-manager.yaml
  - ../../base/cluster-crb-gateway-manager-openshift-gitops-argocd-application-controller.yaml

  # Job to create HostedZone and RecordSet via ACK
  - ../../base/openshift-gitops-job-globex-ns-delegation.yaml

# Minimal DNS delegation test
# Tests only: Job → HostedZone CR → RecordSet CR → DNS delegation
