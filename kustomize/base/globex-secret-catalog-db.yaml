# WARNING: This file contains DEMO database credentials.
# These secrets are FOR TESTING/DEMO PURPOSES ONLY and should NEVER be used in production.
#
# Production deployments should use:
# - Sealed Secrets (bitnami-labs/sealed-secrets)
# - External Secrets Operator (external-secrets/external-secrets)
# - Vault integration
# - Or generate secrets dynamically via Jobs
#
apiVersion: v1
kind: Secret
metadata:
  name: catalog-db
  namespace: globex
  labels:
    app.kubernetes.io/name: catalog-db
    app.kubernetes.io/instance: globex-user1
stringData:
  database-name: catalog
  database-user: catalog
  database-password: catalog  # DEMO SECRET - for testing only
