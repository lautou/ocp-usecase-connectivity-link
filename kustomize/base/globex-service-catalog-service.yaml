apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: globex
  labels:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/instance: globex-user1
spec:
  selector:
    app.kubernetes.io/name: catalog
    app.kubernetes.io/instance: globex-user1
  ports:
    - port: 8080
      targetPort: 8081
