apiVersion: v1
kind: Service
metadata:
  name: prod-web-metrics-proxy
  namespace: ingress-gateway
  labels:
    istio.io/gateway-name: prod-web
spec:
  ports:
    - name: metrics
      port: 15020
      protocol: TCP
      targetPort: 15020
  selector:
    istio.io/gateway-name: prod-web
  type: ClusterIP
